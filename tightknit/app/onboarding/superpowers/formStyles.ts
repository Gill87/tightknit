// Page shell
export const page = 'min-h-[100svh] bg-tk-cream flex flex-col'
export const inner = 'mx-auto w-full max-w-md flex-1 flex flex-col px-6 pt-8 pb-10'

// Progress bar (2 segments)
export const progressBar = 'flex gap-1.5 mb-8'
export const progressSegment = 'h-1 flex-1 rounded-full bg-tk-border'
export const progressSegmentActive = 'h-1 flex-1 rounded-full bg-tk-terracotta'

// Heading
export const heading = 'text-[2rem] font-bold text-tk-forest leading-tight mb-1'
export const subtitle = 'text-[15px] text-tk-muted mb-6'

// Input
export const textInput = 'w-full py-[18px] px-5 rounded-3xl bg-tk-cream-deep text-base text-tk-forest placeholder:text-tk-muted outline-none focus:ring-2 focus:ring-tk-terracotta/30 transition-shadow'

// Hint below input
export const hint = 'text-sm text-tk-muted mt-4 mb-3'

// Preset chips wrap
export const chipsWrap = 'flex flex-wrap gap-2'

// Chip states
export const chipIdle = 'rounded-full border border-tk-border bg-transparent px-4 py-2 text-sm font-medium text-tk-forest transition-colors hover:bg-tk-cream-deep cursor-pointer'
export const chipActive = 'rounded-full bg-tk-terracotta px-4 py-2 text-sm font-semibold text-white cursor-pointer'

// CTA button
export const ctaBase = 'w-full py-4 rounded-full text-base font-semibold transition-colors mt-10'
export const ctaIdle = 'bg-tk-cream-deep text-tk-muted cursor-default'
export const ctaActive = 'bg-tk-terracotta text-white active:scale-[0.98] transition-transform cursor-pointer'
